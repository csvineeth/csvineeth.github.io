git init (in new folder to upload)
git remote add origin https://github.com/csvineeth/csvineeth.github.io.git


TO UPDATE

git add .

git status (to see)

git commit -m "commit message"

git push -u origin main